# Changelog

Changes to `@dsh-std/model` are recorded here.

## 0.1.0-rc2

- Published compatible ranges for internal `@dsh-std` dependencies so Command RC upgrades do not install a second protocol copy.

## 0.1.0-rc1

- Defined model provider resources, model descriptors, status, and catalog access.
- Separated provider publication from profile-specific selection UI.
