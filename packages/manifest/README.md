# @dsh-std/manifest

Static `manifest.yaml` object model and validation for DSH Standard components and facets. `parseManifest()` parses one YAML 1.2 document before applying the manifest schema, while `ManifestDefinitionCatalog` emits structured reports with validator identity, source, digest, and field paths.

See [the design proposal](../../docs/proposals/manifest.zh.md).
