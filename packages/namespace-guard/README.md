# dsh-std

English | [中文](README.zh.md)

The DSH plugin interoperability standard is under active development.

This package protects the unscoped npm project name and exposes no standard runtime API. Protocol packages are published under the `@dsh-std` organization; see the repository [package index](../README.md).
