# Changelog

Changes to the `dsh-std` namespace guard are recorded here.

## 0.1.1-rc.1

- Aligned the namespace guard with the workspace `0.1.1` prerelease line; the unscoped package still exposes no protocol or runtime API.

## 0.0.0-development

- Reserved the unscoped package name and directed consumers to independently versioned `@dsh-std/*` protocol packages.
