{
  "name": "dsh-std-workspace",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm run build && pnpm -r test",
    "check:changelogs": "node scripts/check-changelogs.mjs",
    "check": "pnpm run check:changelogs && pnpm run test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm run build && changeset publish --tag rc"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0",
    "@types/node": "^22.20.0",
    "tsdown": "^0.22.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
